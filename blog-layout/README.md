Usar sass com extensão VSCode

```
scss formatter (shift + alt + f)
live sass compiler

```

Usar sass com NodeJS

```
sass --watch css/input.scss css/output.css
live server (para rodar na porta 5500)
```
